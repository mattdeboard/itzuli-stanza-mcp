# fly.toml app configuration file generated for xingolak on 2026-03-01T22:26:17-06:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'xingolak'
primary_region = 'dfw'

[build]

[deploy]
  strategy = 'bluegreen'

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    grace_period = '300s'
    interval = '15s'
    method = 'GET'
    path = '/health'
    timeout = '10s'

[[vm]]
  memory = '2gb'
  cpus = 1
